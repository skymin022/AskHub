# 기초 설정 빌드 가이드 — 디렉토리 구조 + DB

> 사내문서 RAG 시스템(v3)의 **0단계: 프로젝트 골격** 빌드 가이드.
> Claude Code가 이 문서만 읽고 **디렉토리 구조 + PostgreSQL 초기화**까지 재현하도록 작성됨.
> 이 파일을 레포 루트의 `CLAUDE.md`로 두면 Claude Code가 자동 로드함.
> 설계 근거: `사내문서_RAG_시스템_설계문서_v3.md` (§12.2 구조, §4.2/§15 DB 분리, §13 보안).

---

## 0. 목표

폴리글랏(Spring Boot + Python + nginx) 구조의 **빈 골격**과 **PostgreSQL 스키마/계정 격리**까지만 만든다.
"올려서 검증 가능한 뼈대"가 완성 기준이며, 비즈니스 로직은 다음 단계로 미룬다.

## 1. 스코프 가드 (반드시 지킬 것)

**이번 단계에서 할 것**
- [x] §12.2 디렉토리 트리 생성 (빈 패키지 + 플레이스홀더)
- [x] `docker-compose.yml` — **postgres 서비스만**
- [x] `db/init/` — 스키마(`auth`/`app`) + 서비스 계정 2개 + 기본 테이블 DDL
- [x] `.env.example`
- [x] `rag-service/main.py` — `/api/health`만 (헬스 stub)

**이번 단계에서 하지 말 것 (feature creep 금지)**
- [ ] ❌ Spring Boot 인증/SSO/JWT 발급 로직 구현
- [ ] ❌ RAG 파이프라인(검색·리랭킹·적응형 컷오프·생성) 구현
- [ ] ❌ RAGFlow / Ollama 컨테이너를 compose에 추가
- [ ] ❌ nginx 라우팅 설정 작성
- [ ] ❌ JPA 엔티티 / ORM 모델 / 마이그레이션 툴(Flyway 등) 도입
- [ ] ❌ DDL에 위 표에 없는 테이블·컬럼 추가

> 새 테이블/컬럼이 필요해 보이면 추가하지 말고 **주석으로 TODO만** 남기고 멈춘다.

---

## 2. 최종 디렉토리 구조도

```
ASKHUB/
├── auth-service/                 # ① Spring Boot (인증/식별) — 골격만, 코드 X
│   └── .gitkeep
├── rag-service/                  # ② Python FastAPI (RAG/AI)
│   ├── __init__.py
│   ├── main.py                   # /api/health stub
│   ├── auth/__init__.py          # JWT 검증(JWKS) — 후속
│   ├── acl/__init__.py           # claim → 권한 필터 — 후속
│   ├── ragflow_client/__init__.py# RAGFlow 1차 검색 — 후속
│   ├── rerank/__init__.py        # 리랭커 + 적응형 컷오프 — 후속
│   ├── generate/__init__.py      # 프롬프트·LLM·인용 — 후속
│   └── audit/__init__.py         # 감사 로그 — 후속
├── nginx/                        # 리버스 프록시 — 후속
│   └── .gitkeep
├── eval/                         # RAGAS·한국어 골든셋 — 후속
│   └── .gitkeep
├── models/                       # Ollama pull 스크립트 — 후속
│   └── .gitkeep
├── db/
│   └── init/                     # PostgreSQL 최초 기동 시 1회 실행 (알파벳 순)
│       ├── 01_roles.sh           # 서비스 계정 생성(env에서 비번 주입)
│       ├── 02_auth_schema.sql    # auth 스키마 + 테이블 + GRANT
│       └── 03_app_schema.sql     # app 스키마 + 테이블 + GRANT
├── docker-compose.yml            # 현재는 postgres만
├── .env.example
└── README.md
```

---

## 3. 생성할 파일 (내용 그대로)

### 3.1 `bootstrap.sh`
```bash
#!/usr/bin/env bash
# 사내문서 RAG — 디렉토리 구조 부트스트랩 (설계문서 §12.2 기준)
# 사용: bash bootstrap.sh   (레포 루트에서 실행)
set -euo pipefail

echo "==> 디렉토리 생성"
mkdir -p \
  auth-service \
  rag-service/auth \
  rag-service/acl \
  rag-service/ragflow_client \
  rag-service/rerank \
  rag-service/generate \
  rag-service/audit \
  nginx \
  eval \
  models \
  db/init

echo "==> Python 패키지 마커(__init__.py)"
for d in auth acl ragflow_client rerank generate audit; do
  touch "rag-service/${d}/__init__.py"
done
touch rag-service/__init__.py

echo "==> 빈 디렉토리 유지용 .gitkeep"
for d in auth-service nginx eval models; do
  touch "${d}/.gitkeep"
done

echo "==> 플레이스홀더 파일"
[ -f rag-service/main.py ] || cat > rag-service/main.py <<'PY'
# FastAPI 엔트리포인트 (PB1~). 실제 라우팅은 후속 단계에서 구현.
from fastapi import FastAPI

app = FastAPI(title="internal-docs-rag")


@app.get("/api/health")
def health():
    # 후속: RAGFlow/Ollama/모델/GPU까지 확인하는 deep health (부록 D.2 B4)
    return {"status": "ok"}
PY

echo "==> 완료. 트리:"
find . -maxdepth 3 -not -path '*/.git/*' | sort
```

### 3.2 `.env.example`
```dotenv
# ── PostgreSQL (단일 인스턴스, auth/app 스키마 분리) ──────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-superuser     # 슈퍼유저(초기화 전용, 운영 시 교체)
POSTGRES_DB=ragdb
POSTGRES_PORT=5432

# 서비스별 DB 계정 (스키마 단위 격리 — 부록 A #15)
AUTH_DB_PASSWORD=change-me-auth           # Spring → auth 스키마 전용
APP_DB_PASSWORD=change-me-app             # Python → app 스키마 전용

# ── 서비스 연동 (후속 단계에서 컨테이너 추가 시 사용) ─────────────
RAGFLOW_API=http://ragflow:9380
RAGFLOW_API_TOKEN=                        # A1: Python 우회 차단용 토큰
OLLAMA_URL=http://ollama:11434
JWKS_URL=http://spring-auth:8080/auth/.well-known/jwks.json
RERANKER_DEVICE=cpu                       # cpu | cuda (§7.4 토글)
```

### 3.3 `docker-compose.yml`
```yaml
# 기초 단계: PostgreSQL만 먼저 띄워 스키마/계정 격리를 검증한다.
# 후속 단계에서 nginx / spring-auth / python-rag / ollama / ragflow(공식 compose include)를 추가.
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      # init 스크립트(01_roles.sh)가 참조하는 서비스 계정 비밀번호
      AUTH_DB_PASSWORD: ${AUTH_DB_PASSWORD}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      # 최초 기동 시 알파벳 순(01→02→03)으로 1회 실행
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pg_data:
```

### 3.4 `db/init/01_roles.sh`
```bash
#!/bin/bash
# 서비스별 DB 계정 생성 — 비밀번호는 env에서 주입(SQL 파일에 시크릿 하드코딩 금지, §13)
set -e

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
  -- LOGIN 계정. 스키마 GRANT는 02/03 SQL에서 각각 부여(스키마 단위 격리).
  CREATE ROLE auth_service LOGIN PASSWORD '${AUTH_DB_PASSWORD}';
  CREATE ROLE app_service  LOGIN PASSWORD '${APP_DB_PASSWORD}';

  -- 기본 스키마(public) 무권한화: 격리 강제
  REVOKE ALL ON SCHEMA public FROM auth_service, app_service;
EOSQL
```

### 3.5 `db/init/02_auth_schema.sql`
```sql
-- ════════════════════════════════════════════════════════════
--  auth 스키마 — Spring Boot 소유 (users, roles, departments, credentials)
--  JWT claim의 roles[], department[]가 배열이므로 사용자↔역할/부서는 다대다.
-- ════════════════════════════════════════════════════════════
CREATE SCHEMA IF NOT EXISTS auth;

-- 부서: 'security', 'all'(전사 공개) 등 (§8.1)
CREATE TABLE auth.departments (
    id          BIGINT GENERATED ALWAYS AS IDENTITY,
    code        VARCHAR(64)  NOT NULL UNIQUE,
    name        VARCHAR(128) NOT NULL,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now(),
    PRIMARY KEY (id)
);

-- 역할: 'employee', 'admin' 등
CREATE TABLE auth.roles (
    id          BIGINT GENERATED ALWAYS AS IDENTITY,
    code        VARCHAR(64)  NOT NULL UNIQUE,
    name        VARCHAR(128) NOT NULL,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now(),
    PRIMARY KEY (id)
);

-- 사용자: external_id = JWT sub(사번/계정 ID)
CREATE TABLE auth.users (
    id            BIGINT GENERATED ALWAYS AS IDENTITY,
    external_id   VARCHAR(128) NOT NULL UNIQUE,   -- JWT 'sub'
    email         VARCHAR(255) NOT NULL UNIQUE,
    display_name  VARCHAR(128) NOT NULL,
    status        VARCHAR(32)  NOT NULL DEFAULT 'active', -- active/suspended/left (A2)
    created_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    PRIMARY KEY (id)
);

-- 로컬/SSO 자격증명. SSO 전용이면 password_hash는 NULL.
CREATE TABLE auth.credentials (
    user_id        BIGINT      NOT NULL,
    provider       VARCHAR(32) NOT NULL DEFAULT 'local',  -- local/oidc/saml
    password_hash  VARCHAR(255),                          -- bcrypt/argon2 (local만)
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (user_id),
    FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE
);

-- 사용자 ↔ 역할 (다대다)
CREATE TABLE auth.user_roles (
    user_id  BIGINT NOT NULL,
    role_id  BIGINT NOT NULL,
    PRIMARY KEY (user_id, role_id),
    FOREIGN KEY (user_id) REFERENCES auth.users(id)  ON DELETE CASCADE,
    FOREIGN KEY (role_id) REFERENCES auth.roles(id)  ON DELETE CASCADE
);

-- 사용자 ↔ 부서 (다대다)
CREATE TABLE auth.user_departments (
    user_id        BIGINT NOT NULL,
    department_id  BIGINT NOT NULL,
    PRIMARY KEY (user_id, department_id),
    FOREIGN KEY (user_id)       REFERENCES auth.users(id)        ON DELETE CASCADE,
    FOREIGN KEY (department_id) REFERENCES auth.departments(id)  ON DELETE CASCADE
);

-- 시드(개발용 최소값)
INSERT INTO auth.departments (code, name) VALUES
    ('all', '전사 공개'), ('security', '보안팀'), ('hr', '인사팀');
INSERT INTO auth.roles (code, name) VALUES
    ('employee', '일반 직원'), ('admin', '관리자');

-- ── 권한: auth_service는 auth 스키마만 ──────────────────────────
GRANT USAGE ON SCHEMA auth TO auth_service;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES    IN SCHEMA auth TO auth_service;
GRANT USAGE, SELECT                  ON ALL SEQUENCES IN SCHEMA auth TO auth_service;
ALTER DEFAULT PRIVILEGES IN SCHEMA auth
    GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES    TO auth_service;
ALTER DEFAULT PRIVILEGES IN SCHEMA auth
    GRANT USAGE, SELECT                  ON SEQUENCES TO auth_service;
```

### 3.6 `db/init/03_app_schema.sql`
```sql
-- ════════════════════════════════════════════════════════════
--  app 스키마 — Python(FastAPI) 소유 (audit_log, query_history)
--  user_sub는 JWT 'sub' 문자열만 보관(스키마 격리 위해 auth로 FK 걸지 않음).
-- ════════════════════════════════════════════════════════════
CREATE SCHEMA IF NOT EXISTS app;

-- 질의 이력
CREATE TABLE app.query_history (
    id               BIGINT GENERATED ALWAYS AS IDENTITY,
    correlation_id   UUID         NOT NULL,            -- 폴리글랏 추적 ID (D2)
    user_sub         VARCHAR(128) NOT NULL,            -- JWT sub
    question         TEXT         NOT NULL,            -- PII 가능 → 마스킹/보존 정책(D2)
    answer           TEXT,
    selected_chunks  INT,
    reranker_device  VARCHAR(16),
    model            VARCHAR(64),
    latency_ms       INT,
    created_at       TIMESTAMPTZ  NOT NULL DEFAULT now(),
    PRIMARY KEY (id)
);

-- 감사 로그: 질의·근거 문서 접근 이력 (§13)
CREATE TABLE app.audit_log (
    id              BIGINT GENERATED ALWAYS AS IDENTITY,
    correlation_id  UUID         NOT NULL,
    user_sub        VARCHAR(128) NOT NULL,
    event_type      VARCHAR(64)  NOT NULL,             -- query/doc_access/upload/delete...
    doc_id          VARCHAR(128),                      -- 접근한 근거 문서
    detail          JSONB,
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT now(),
    PRIMARY KEY (id)
);

CREATE INDEX idx_query_history_user_time ON app.query_history (user_sub, created_at DESC);
CREATE INDEX idx_audit_correlation       ON app.audit_log (correlation_id);
CREATE INDEX idx_audit_user_time         ON app.audit_log (user_sub, created_at DESC);

-- ── 권한: app_service는 app 스키마만 ───────────────────────────
GRANT USAGE ON SCHEMA app TO app_service;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES    IN SCHEMA app TO app_service;
GRANT USAGE, SELECT                  ON ALL SEQUENCES IN SCHEMA app TO app_service;
ALTER DEFAULT PRIVILEGES IN SCHEMA app
    GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES    TO app_service;
ALTER DEFAULT PRIVILEGES IN SCHEMA app
    GRANT USAGE, SELECT                  ON SEQUENCES TO app_service;
```

---

## 4. 실행 절차

```bash
# 1) 디렉토리 골격
bash bootstrap.sh

# 2) db/init/* 실행 권한
chmod +x db/init/01_roles.sh

# 3) env 준비 (비밀번호 4개 채우기)
cp .env.example .env

# 4) DB 기동 (init 스크립트는 최초 1회만 실행됨)
docker compose up -d postgres
```

> ⚠️ init 스크립트는 **데이터 볼륨이 비어 있을 때만** 실행된다. DDL을 고친 뒤 다시 적용하려면
> `docker compose down -v` 로 `pg_data` 볼륨을 지우고 재기동한다.

---

## 5. 검증 게이트 (Definition of Done)

아래가 모두 통과해야 0단계 완료:

```bash
# (1) 스키마 2개 존재: auth, app
docker compose exec postgres psql -U postgres -d ragdb -c '\dn'

# (2) auth 테이블 6개: users, roles, departments, credentials, user_roles, user_departments
docker compose exec postgres psql -U postgres -d ragdb -c '\dt auth.*'

# (3) app 테이블 2개: query_history, audit_log
docker compose exec postgres psql -U postgres -d ragdb -c '\dt app.*'

# (4) 시드 확인: 부서 3행, 역할 2행
docker compose exec postgres psql -U postgres -d ragdb -c 'SELECT code FROM auth.roles;'

# (5) 격리 검증: app_service가 auth 테이블 접근 시 'permission denied'가 나야 정상
docker compose exec postgres psql -U app_service -d ragdb -c 'SELECT * FROM auth.users;'
#   → ERROR: permission denied for schema auth  (이게 정상)

# (6) 반대로 app_service는 app 테이블 접근 가능해야 함
docker compose exec postgres psql -U app_service -d ragdb -c 'SELECT count(*) FROM app.query_history;'
#   → 0  (정상)
```

체크리스트:
- [ ] (1) `auth`, `app` 두 스키마 표시
- [ ] (2) auth 테이블 6개
- [ ] (3) app 테이블 2개
- [ ] (4) 시드 데이터 존재
- [ ] (5) `app_service` → `auth` 접근이 **거부**됨 (격리 성공)
- [ ] (6) `app_service` → `app` 접근은 **성공**

---

## 6. 미해결(다음 단계로 넘김 — 여기서 구현 금지)

- **로컬 로그인 vs SSO 전용**: `credentials.provider`로 둘 다 수용 중. 정책 확정 시 단순화 가능.
- **Spring `auth-service` 골격**: JWT RS256 발급 + JWKS (SA1~SA4 트랙).
- **Python `rag-service` DB 레이어**: `app_service` 커넥션 + audit/query insert (PB 트랙).
- **마이그레이션 툴**: 지금은 init SQL 1회 실행. 스키마 변경이 잦아지면 Flyway/Alembic 도입 검토.
